<Window x:Class="DesktopSpike.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="小狗效率屋"
        Width="330" Height="390" MinWidth="280" MinHeight="320"
        WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="Manual"
        ShowInTaskbar="True" Topmost="True" Background="#F3F6F2"
        FontFamily="Microsoft YaHei UI" TextOptions.TextFormattingMode="Display" UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="18"
                                  GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="InkBrush" Color="#515B53" />
        <SolidColorBrush x:Key="MutedBrush" Color="#7C847D" />
        <SolidColorBrush x:Key="PaperBrush" Color="#F3F6F2" />
        <SolidColorBrush x:Key="CardBrush" Color="#FFFEFB" />
        <SolidColorBrush x:Key="AccentBrush" Color="#DDE9DF" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#CFDFD2" />
        <SolidColorBrush x:Key="AccentStrongBrush" Color="#718679" />
        <SolidColorBrush x:Key="LineBrush" Color="#CDD8CF" />
        <SolidColorBrush x:Key="SoftBrush" Color="#EAF1EB" />
        <SolidColorBrush x:Key="DangerBrush" Color="#A36F67" />

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFFEFB" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="9,7" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentStrongBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox">
                <Border x:Name="TextBoxBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" SnapsToDevicePixels="True">
                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{StaticResource AccentStrongBrush}" /><Setter TargetName="TextBoxBorder" Property="Background" Value="#FFFFFF" /></Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter TargetName="TextBoxBorder" Property="Opacity" Value="0.55" /></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="28" /><Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="3,0,0,0" /><Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#FAFCF9" /><Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
            <Setter Property="BorderThickness" Value="1" /><Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontSize" Value="13" /><Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="9">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentBrush}" /><Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#AFC0B3" /></Trigger>
                    <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AccentHoverBrush}" /></Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45" /></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentStrongBrush}" /><Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" /><Setter Property="Padding" Value="13,7" />
            <Setter Property="FontSize" Value="11.5" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="PrimaryBorder" Background="{TemplateBinding Background}" CornerRadius="9" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="PrimaryBorder" Property="Background" Value="#607668" /></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="PrimaryBorder" Property="Opacity" Value="0.82" /></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>

        <Style x:Key="SoftButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SoftBrush}" /><Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" /><Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" /><Setter Property="Margin" Value="4,0,0,0" /><Setter Property="FontSize" Value="10.5" /><Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="SoftBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="SoftBorder" Property="Background" Value="{StaticResource AccentBrush}" /></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="SoftBorder" Property="Background" Value="{StaticResource AccentHoverBrush}" /></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>

        <Style TargetType="TabControl"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /></Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" /><Setter Property="FontSize" Value="11.5" /><Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="11,6" /><Setter Property="Margin" Value="0,0,5,0" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem">
                <Border x:Name="TabBorder" Background="#EEF3EE" CornerRadius="9" Padding="{TemplateBinding Padding}"><ContentPresenter ContentSource="Header" HorizontalAlignment="Center" /></Border>
                <ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="TabBorder" Property="Background" Value="{StaticResource AccentBrush}" /><Setter Property="Foreground" Value="{StaticResource InkBrush}" /></Trigger><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TabBorder" Property="Background" Value="#E3ECE4" /></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="ListBox"><Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" /></Style>
        <Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Padding" Value="0" /><Setter Property="Margin" Value="0" /><Setter Property="Background" Value="Transparent" /><Setter Property="BorderThickness" Value="0" /><Setter Property="Focusable" Value="False" /></Style>
    </Window.Resources>

    <Grid>
        <Border x:Name="CollapsedPanel" Visibility="Collapsed" Margin="3" Background="#FFFEFB" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="22" MouseLeftButtonDown="CollapsedDrag_MouseLeftButtonDown" Cursor="SizeAll">
            <Border.Effect><DropShadowEffect BlurRadius="13" ShadowDepth="2" Opacity="0.18" Color="#526055" /></Border.Effect>
            <Grid><Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <Button Background="Transparent" BorderThickness="0" Click="Expand_Click" Cursor="Hand" ToolTip="展开面板">
                    <Grid Width="46" Height="46"><Ellipse Fill="#FFFEFB" Stroke="{StaticResource LineBrush}" StrokeThickness="1.3" /><Ellipse Margin="3"><Ellipse.Fill><ImageBrush ImageSource="pack://application:,,,/HMinus.DesktopSpike;component/Assets/dog-reference.jpg" Stretch="UniformToFill" /></Ellipse.Fill></Ellipse></Grid>
                </Button>
                <TextBlock Grid.Row="1" Text="展开" HorizontalAlignment="Center" Margin="0,0,0,9" FontSize="9.5" Foreground="{StaticResource MutedBrush}" />
            </Grid>
        </Border>

        <Border x:Name="ExpandedPanel" Margin="4" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="18" ClipToBounds="True">
            <Border.Effect><DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.17" Color="#536158" /></Border.Effect>
            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <Border Background="#F8FAF7" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="10,9" MouseLeftButtonDown="HeaderDrag_MouseLeftButtonDown" Cursor="SizeAll">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <Grid Width="43" Height="43"><Ellipse Fill="#FFFEFB" Stroke="{StaticResource LineBrush}" StrokeThickness="1.2" /><Ellipse Margin="3"><Ellipse.Fill><ImageBrush ImageSource="pack://application:,,,/HMinus.DesktopSpike;component/Assets/dog-reference.jpg" Stretch="UniformToFill" /></Ellipse.Fill></Ellipse></Grid>
                        <StackPanel Grid.Column="1" Margin="9,2,6,0" VerticalAlignment="Center"><TextBlock x:Name="DisplayNameText" Text="小狗效率屋" FontSize="14.5" FontWeight="Bold" Foreground="{StaticResource InkBrush}" TextTrimming="CharacterEllipsis" /><TextBlock Text="v1.0.0 · 本机待办与剪贴板" Margin="0,2,0,0" FontSize="9.5" Foreground="{StaticResource MutedBrush}" /></StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"><Button Style="{StaticResource IconButton}" Content="⚙" ToolTip="设置名称" Click="OpenSettings_Click" /><Button Style="{StaticResource IconButton}" Content="↺" ToolTip="重置窗口位置和大小" Click="ResetWindow_Click" /><Button Style="{StaticResource IconButton}" Content="—" ToolTip="收起为小狗贴纸" Click="Collapse_Click" /><Button Style="{StaticResource IconButton}" Content="×" ToolTip="关闭并退出" Click="Close_Click" /></StackPanel>
                    </Grid>
                </Border>

                <TabControl Grid.Row="1" Margin="10,9,10,7">
                    <TabItem Header="待办"><Grid Margin="0,8,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <Border Background="#F2F6F1" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="7"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBox x:Name="TodoInput" ToolTip="输入待办，按Enter也可添加" VerticalContentAlignment="Center" KeyDown="TodoInput_KeyDown" /><Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="添加" Margin="7,0,0,0" Click="AddTodo_Click" /></Grid></Border>
                        <TextBlock x:Name="TodoPersistenceText" Grid.Row="1" Text="正在准备本机保存…" Margin="3,5,0,4" FontSize="9.5" Foreground="{StaticResource MutedBrush}" />
                        <TabControl Grid.Row="2" x:Name="TodoSectionTabs">
                            <TabItem Header="今日待办"><Grid Margin="0,7,0,0">
                                <TextBlock x:Name="CurrentTodoEmptyState" Text="今天没有待办，和小狗一起轻松一下吧。" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                                <ListBox ItemsSource="{Binding CurrentTodoItemsView}" ScrollViewer.VerticalScrollBarVisibility="Auto"><ListBox.ItemTemplate><DataTemplate>
                                    <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="8" Margin="0,0,0,6"><Grid>
                                        <Grid x:Name="TodoDisplayRow"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}" Margin="1,2,8,0" VerticalAlignment="Top" Checked="TodoCompletion_Changed" Unchecked="TodoCompletion_Changed" ToolTip="标记完成" />
                                            <StackPanel Grid.Column="1"><TextBlock Text="{Binding Text}" TextWrapping="Wrap" Foreground="{StaticResource InkBrush}" FontSize="11.5" /><TextBlock Text="{Binding CreatedDateLabel}" Margin="0,3,0,0" Foreground="{StaticResource MutedBrush}" FontSize="9" /></StackPanel>
                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top"><Button Style="{StaticResource SoftButton}" Content="编辑" Tag="{Binding}" ToolTip="编辑待办" Click="EditTodo_Click" /><Button Style="{StaticResource SoftButton}" Content="删除" Tag="{Binding}" ToolTip="删除待办" Click="DeleteTodo_Click" /></StackPanel>
                                        </Grid>
                                        <Grid x:Name="TodoEditRow" Visibility="Collapsed"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions><TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}" KeyDown="TodoEdit_KeyDown" Tag="{Binding}" /><StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0"><Button Style="{StaticResource SoftButton}" Content="保存" Tag="{Binding}" Click="SaveTodo_Click" /><Button Style="{StaticResource SoftButton}" Content="取消" Tag="{Binding}" Click="CancelTodo_Click" /></StackPanel></Grid>
                                    </Grid></Border>
                                    <DataTemplate.Triggers><DataTrigger Binding="{Binding IsEditing}" Value="True"><Setter TargetName="TodoDisplayRow" Property="Visibility" Value="Collapsed" /><Setter TargetName="TodoEditRow" Property="Visibility" Value="Visible" /></DataTrigger></DataTemplate.Triggers>
                                </DataTemplate></ListBox.ItemTemplate></ListBox>
                            </Grid></TabItem>
                            <TabItem Header="完成历史"><Grid Margin="0,7,0,0">
                                <TextBlock x:Name="CompletedTodoEmptyState" Text="还没有完成记录，完成一件小事也值得庆祝。" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                                <ListBox ItemsSource="{Binding CompletedTodoItemsView}" ScrollViewer.VerticalScrollBarVisibility="Auto"><ListBox.GroupStyle><GroupStyle><GroupStyle.HeaderTemplate><DataTemplate><Border Background="{StaticResource AccentBrush}" CornerRadius="8" Padding="8,4" Margin="0,3,0,6"><TextBlock Text="{Binding Name}" Foreground="{StaticResource InkBrush}" FontSize="10.5" FontWeight="SemiBold" /></Border></DataTemplate></GroupStyle.HeaderTemplate></GroupStyle></ListBox.GroupStyle>
                                    <ListBox.ItemTemplate><DataTemplate><Border Background="#FAFCF9" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="8" Margin="0,0,0,6"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Text}" TextWrapping="Wrap" TextDecorations="Strikethrough" Foreground="{StaticResource MutedBrush}" FontSize="11.5" /><TextBlock Text="{Binding CompletedTimeLabel, StringFormat=完成于 {0}}" Margin="0,3,0,0" Foreground="{StaticResource MutedBrush}" FontSize="9" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top"><Button Style="{StaticResource SoftButton}" Content="恢复" Tag="{Binding}" ToolTip="恢复为未完成" Click="RestoreTodo_Click" /><Button Style="{StaticResource SoftButton}" Content="删除" Tag="{Binding}" ToolTip="删除完成记录" Click="DeleteTodo_Click" /></StackPanel></Grid></Border></DataTemplate></ListBox.ItemTemplate>
                                </ListBox>
                            </Grid></TabItem>
                        </TabControl>
                    </Grid></TabItem>

                    <TabItem Header="剪贴板"><Grid Margin="0,8,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                        <Border Background="#F2F6F1" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="7"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBox x:Name="ClipboardSearchBox" ToolTip="搜索完整文字内容" TextChanged="ClipboardSearch_TextChanged" VerticalContentAlignment="Center" /><Button Grid.Column="1" Style="{StaticResource SoftButton}" Content="清除" Margin="7,0,0,0" Click="ClearSearch_Click" /></Grid></Border>
                        <Grid Grid.Row="1" Margin="3,5,2,2"><TextBlock x:Name="SearchCountText" Foreground="{StaticResource MutedBrush}" FontSize="9.5" /><TextBlock Text="支持逐条管理" HorizontalAlignment="Right" Foreground="{StaticResource MutedBrush}" FontSize="9.5" /></Grid>
                        <TextBlock x:Name="ClipboardPersistenceText" Grid.Row="2" Text="正在准备文字本机保存…" Margin="3,0,2,5" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="9" />
                        <Grid Grid.Row="3"><TextBlock x:Name="ClipboardEmptyState" Text="复制文字或图片后，会显示在这里。" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="18" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                            <ListBox ItemsSource="{Binding ClipboardEventsView}" ScrollViewer.VerticalScrollBarVisibility="Auto"><ListBox.ItemTemplate><DataTemplate>
                                <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="8" Margin="0,0,0,6"><Grid>
                                    <Grid x:Name="ClipboardDisplayRow"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                        <Border Background="{StaticResource AccentBrush}" CornerRadius="8" Padding="7,4" VerticalAlignment="Top"><TextBlock Text="{Binding KindLabel}" Foreground="{StaticResource InkBrush}" FontWeight="SemiBold" FontSize="9.5" /></Border>
                                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                            <Border x:Name="ImagePreviewBorder" Visibility="Collapsed" Height="112" Margin="0,0,0,7" Background="#EEF3EE" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" ClipToBounds="True"><Image Source="{Binding Snapshot.ImagePayload}" Stretch="Uniform" Margin="4" /></Border>
                                            <TextBlock Text="{Binding ImageStatusText}" TextWrapping="Wrap" MaxHeight="55" Foreground="{StaticResource InkBrush}" FontSize="11.5" />
                                            <Grid Margin="0,5,0,0"><TextBlock Text="{Binding TimeLabel}" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" /><Border x:Name="PinnedBadge" Visibility="Collapsed" HorizontalAlignment="Right" Background="#E3EEE4" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="7" Padding="6,2"><TextBlock Text="已置顶" Foreground="{StaticResource InkBrush}" FontSize="8.5" FontWeight="SemiBold" /></Border></Grid>
                                            <WrapPanel HorizontalAlignment="Right" Margin="0,5,0,0">
                                                <Button Style="{StaticResource SoftButton}" Content="复制" Tag="{Binding}" ToolTip="重新复制这条内容" Click="CopyClipboardItem_Click" />
                                                <Button x:Name="EditClipboardButton" Style="{StaticResource SoftButton}" Content="编辑" Tag="{Binding}" ToolTip="编辑这条文字" Click="EditClipboardItem_Click" />
                                                <Button x:Name="PinClipboardButton" Style="{StaticResource SoftButton}" Content="置顶" Tag="{Binding}" ToolTip="置顶或取消置顶这条记录" Click="ToggleClipboardPin_Click" />
                                                <Button x:Name="DeleteClipboardButton" Style="{StaticResource SoftButton}" Content="删除" Tag="{Binding}" ToolTip="删除这条记录" Click="DeleteClipboardItem_Click" />
                                            </WrapPanel>
                                        </StackPanel>
                                    </Grid>
                                    <Grid x:Name="ClipboardEditRow" Visibility="Collapsed"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions><TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}" Tag="{Binding}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="58" MaxHeight="115" VerticalScrollBarVisibility="Auto" KeyDown="ClipboardEdit_KeyDown" /><StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0"><TextBlock Text="Ctrl+Enter保存" Foreground="{StaticResource MutedBrush}" FontSize="9" VerticalAlignment="Center" Margin="0,0,5,0" /><Button Style="{StaticResource SoftButton}" Content="保存" Tag="{Binding}" Click="SaveClipboardEdit_Click" /><Button Style="{StaticResource SoftButton}" Content="取消" Tag="{Binding}" Click="CancelClipboardEdit_Click" /></StackPanel></Grid>
                                </Grid></Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsEditing}" Value="True"><Setter TargetName="ClipboardDisplayRow" Property="Visibility" Value="Collapsed" /><Setter TargetName="ClipboardEditRow" Property="Visibility" Value="Visible" /></DataTrigger>
                                    <DataTrigger Binding="{Binding IsText}" Value="False"><Setter TargetName="ImagePreviewBorder" Property="Visibility" Value="Visible" /><Setter TargetName="EditClipboardButton" Property="Visibility" Value="Collapsed" /></DataTrigger>
                                    <DataTrigger Binding="{Binding IsPinned}" Value="True"><Setter TargetName="PinnedBadge" Property="Visibility" Value="Visible" /><Setter TargetName="PinClipboardButton" Property="Content" Value="取消置顶" /></DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate></ListBox.ItemTemplate></ListBox>
                        </Grid>
                    </Grid></TabItem>
                </TabControl>
                <Border Grid.Row="2" Background="#EDF3ED" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0" Padding="10,7"><TextBlock x:Name="StatusText" Text="正在初始化…" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" FontSize="10" /></Border>
            </Grid>
        </Border>

        <Grid x:Name="SettingsOverlay" Visibility="Collapsed" Panel.ZIndex="20" Background="#66000000">
            <Border Width="270" MaxWidth="290" Background="#FFFEFB" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="16" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect><DropShadowEffect BlurRadius="22" ShadowDepth="4" Opacity="0.25" /></Border.Effect>
                <StackPanel><Grid><TextBlock Text="个性化设置" FontSize="15" FontWeight="Bold" Foreground="{StaticResource InkBrush}" /><Button HorizontalAlignment="Right" Style="{StaticResource IconButton}" Content="×" ToolTip="关闭设置" Click="CloseSettings_Click" /></Grid>
                    <TextBlock Text="工具名称" Margin="0,15,0,5" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource InkBrush}" /><TextBox x:Name="SettingsNameTextBox" MaxLength="24" ToolTip="最多24个字符" KeyDown="SettingsName_KeyDown" /><TextBlock Text="名称会显示在页头、任务栏和折叠贴纸提示中。" TextWrapping="Wrap" Margin="0,6,0,0" FontSize="9.5" Foreground="{StaticResource MutedBrush}" /><TextBlock x:Name="SettingsValidationText" Text="" TextWrapping="Wrap" Margin="0,6,0,0" FontSize="9.5" Foreground="{StaticResource DangerBrush}" />
                    <Grid Margin="0,14,0,0"><Button Style="{StaticResource SoftButton}" Content="恢复默认" HorizontalAlignment="Left" Click="RestoreDefaultName_Click" /><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Style="{StaticResource SoftButton}" Content="取消" Click="CloseSettings_Click" /><Button Style="{StaticResource PrimaryButton}" Content="保存" Margin="6,0,0,0" Click="SaveSettings_Click" /></StackPanel></Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="PrivacyOverlay" Visibility="Collapsed" Panel.ZIndex="30" Background="#77000000">
            <Border Width="274" MaxWidth="290" Background="#FFFEFB" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="16" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect><DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.28" /></Border.Effect>
                <StackPanel>
                    <TextBlock Text="启用剪贴板记录前，请先了解" FontSize="14.5" FontWeight="Bold" Foreground="{StaticResource InkBrush}" />
                    <Border Background="#EEF4EE" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,11,0,0">
                        <StackPanel>
                            <TextBlock Text="• 运行期间复制的文字和图片会保存在这台电脑。" TextWrapping="Wrap" Foreground="{StaticResource InkBrush}" FontSize="10.5" Margin="0,0,0,5" />
                            <TextBlock Text="• 密码、验证码、密钥等敏感文字也可能被记录。" TextWrapping="Wrap" Foreground="{StaticResource InkBrush}" FontSize="10.5" Margin="0,0,0,5" />
                            <TextBlock Text="• 数据不会上传云端；退出软件后停止监听。" TextWrapping="Wrap" Foreground="{StaticResource InkBrush}" FontSize="10.5" Margin="0,0,0,5" />
                            <TextBlock Text="• 已保存文字和图片不会因为退出软件而自动消失。" TextWrapping="Wrap" Foreground="{StaticResource InkBrush}" FontSize="10.5" />
                        </StackPanel>
                    </Border>
                    <TextBlock Text="支持逐条管理文字和图片；本版暂未提供自动到期，请谨慎保存敏感内容。" TextWrapping="Wrap" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" FontSize="9.5" />
                    <TextBlock x:Name="PrivacyValidationText" Text="" TextWrapping="Wrap" Margin="0,6,0,0" Foreground="{StaticResource DangerBrush}" FontSize="9.5" />
                    <Grid Margin="0,14,0,0"><Button Style="{StaticResource SoftButton}" Content="暂不启用" HorizontalAlignment="Left" Click="DeclinePrivacy_Click" /><Button Style="{StaticResource PrimaryButton}" Content="我已了解并启用" HorizontalAlignment="Right" Click="AcceptPrivacy_Click" /></Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>



