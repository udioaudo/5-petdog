# 开发日志：2026-07-12

## 今日目标

- [x] 完成第一版产品需求、项目文档治理和每日留痕体系。
- [x] 完成阶段0首个Windows技术原型并生成第一版体验包。
- [x] 根据用户反馈完成阶段0第二版自由窗口与黑白线条小狗UI。
- [x] 完成阶段0第三版参考图视觉、剪贴板搜索和内存待办体验。

## 已完成事项

- 完成第一版产品需求和开发标准文档；
- 建立.NET 10.0.301 + WPF阶段0原型、构建测试发布脚本；
- 完成文字和图片剪贴板监听、重复抑制、文件忽略和自身复制回流保护；
- 将窗口从固定右侧全高面板改为可自由拖动、四边和四角缩放的悬浮面板；
- 默认宽330 DIP、高度约为工作区三分之一，最低可用尺寸280×320 DIP；
- 完成56×96 DIP折叠小狗贴纸，贴纸可拖动，点击后恢复原尺寸；
- 完成展开位置、尺寸和显示器名称的本机设置保存，重启后默认展开；
- 完成设置损坏、显示器移除和窗口越界的安全回退规则；
- 增加“重置窗口”功能，恢复主屏幕右下默认位置和尺寸；
- 完成黑白极简线条小狗贴纸UI，全部图形使用本地WPF矢量；
- 保留第一版体验包，生成独立第二版自包含体验包；
- 将 `E:\7.12.jpg` 复制并嵌入程序资源，替换原黑白矢量小狗；
- 完成浅绿、暖灰、米白配色改版；
- 增加文字剪贴板搜索、清除搜索、结果数量和无结果状态；
- 增加内存待办的添加、编辑、完成、取消完成、删除、排序和空状态；
- 待办与剪贴板内容继续保持不落盘，退出后清空；
- 更新产品需求、路线图、界面规范、技术决策、代理工作说明和体验说明。

## 验证与测试

- Debug和Release构建均为0警告、0错误；
- 36项自动验证通过，新增覆盖搜索匹配、图片搜索排除、待办输入规范化和属性通知；
- 自动化窗口烟雾测试通过：展开物理尺寸413×400、折叠70×120、点击贴纸恢复413×400；当前系统缩放约为125%；
- 验证关闭时处于折叠状态，重新启动仍以展开状态恢复；
- 验证移动和缩放后的几何信息可在下一次启动恢复；
- 第二版win-x64自包含EXE可不依赖项目本地SDK独立启动，并可通过关闭按钮彻底退出；
- 第二版EXE SHA-256：`71A59FE750B72A98C36AF8F15D9FDDF86B9170C3DF90AFC9EA84E6C6510E1645`；
- 设置文件检查通过，只包含Left、Top、Width、Height和MonitorDeviceName，不包含剪贴板内容。
- 第三版自包含EXE烟雾测试通过：启动、添加待办、文字搜索、折叠展开和关闭退出均正常；
- 第三版EXE SHA-256：`5F6610A135CFACD30FC1F430F9B14F9AE873D4D44F75F37D8A8F7E61419FF172`；

## 决策与变更

- 用户最新确认：面板不固定，可自由拖动、缩放并记忆位置和尺寸；
- 用户最新确认：折叠贴纸留在当前位置，重启恢复位置和大小但默认展开；
- 用户否定黑白矢量小狗方向；第三版改为直接使用参考图并承接浅绿、暖灰、米白配色；
- 位置设置保存在 `%LocalAppData%\HMinus\DesktopSpike\window-settings.json`；
- 窗口设置持久化不改变阶段0剪贴板内容不落盘的安全边界；
- 更新产品需求、路线图、技术规范、界面规范、安全规范和技术决策记录。

## 风险与问题

- 当前只在本机当前缩放环境完成真实窗口测试；100%、150%、200%及混合DPI双显示器仍需人工验证；
- 多显示器恢复逻辑已有自动规则测试，但真实显示器插拔尚未手工验证；
- SQLite、图片文件生命周期和超大图片性能尚未验证，因此不得进入阶段1；
- 极端快速剪贴板变化仍可能跳过中间瞬时内容；
- 原型仍不持久化待办和剪贴板历史，退出后全部清空。

## 待办事项

- [ ] 请用户体验第三版参考图视觉、待办操作和剪贴板搜索；
- [ ] 根据用户实际缩放和多显示器环境反馈修正窗口细节；
- [ ] 用户确认窗口体验后，只验证本地SQLite和图片文件生命周期；
- [ ] 本地存储验证通过前不创建正式应用工程。

## 结束状态

- 当前阶段：阶段0进行中；窗口、剪贴板监听、第三版视觉、搜索和内存待办交互已验证，本地存储验证待完成；
- 是否可构建：是，Debug/Release均为0警告0错误；
- 是否可测试：是，36项自动验证以及待办、搜索、折叠展开窗口烟雾测试通过；
- 是否可体验：是，第三版自包含体验包已生成；
- 建议下一步：先收集用户对第三版视觉、待办和搜索交互的反馈，再进入本地存储可行性验证。






## 阶段1追加记录

### 今日目标

- [x] 用户确认阶段0结束，进入阶段1工程骨架与数据层；
- [x] 只完成SQLite骨架与待办本机持久化最小闭环，不持久化剪贴板内容。

### 已完成事项

- 引入SQLite版本化初始化，数据库路径为 `%LocalAppData%\HMinus\DesktopSpike\data\hminus.db`；
- 使用 `PRAGMA user_version=1` 和事务创建 `todos` 表；
- 完成待办新增、重载、编辑、完成、取消完成和删除的本机持久化；
- 待办按未完成优先、组内创建时间降序读取；
- 数据库初始化失败时回退内存模式，应用仍可启动；
- 损坏数据库不会被自动删除或替换；
- 写入失败时采用失败安全行为：编辑不覆盖、完成状态回退、删除保留；
- 剪贴板内容继续仅存在内存中，数据库没有剪贴板表；
- 为避免引入已报告高危漏洞的SQLite原生包，改用Windows系统SQLite绑定；
- 新增 `docs/09-技术决策-002-SQLite本地数据层.md` 并更新路线图、技术和安全规范。

### 验证与测试

- Release构建0警告、0错误；
- 48项自动验证通过；
- 新增覆盖数据库版本、幂等初始化、待办字段往返、排序、编辑、完成、删除、损坏文件保护和内存回退；
- NuGet还原和构建未报告已知高危依赖警告；
- 阶段1自包含体验包发布成功；自动烟雾测试验证启动、新增待办、关闭、重启恢复和再次关闭；
- 阶段1EXE SHA-256：`1D308B369C973A67701AB6B2AE9FED3B471BBF021AA28C3016029C170B2262FD`。

### 决策与变更

- 用户于2026-07-12明确确认阶段0结束并进入下一阶段；
- 阶段1首个增量只持久化待办，不同时推进剪贴板数据、图片生命周期、自动清理和置顶；
- 阶段0中尚未单独验证的图片文件生命周期移至图片剪贴板阶段处理。

### 风险与问题

- 当前使用Windows系统SQLite，符合仅支持Windows 10/11的产品范围；
- 尚未实现正式数据恢复界面、备份、导入导出或加密；
- 剪贴板仍会在退出时清空，本增量不改变敏感剪贴板数据边界。

### 待办事项

- [x] 发布阶段1首个自包含体验包并验证待办重启恢复；
- [ ] 用户体验确认后，再推进阶段1下一小增量；
- [ ] 剪贴板持久化和图片文件生命周期保持待办，不在当前增量实现。

### 结束状态

- 当前阶段：阶段1进行中；首个SQLite与待办持久化增量已完成并通过自动与烟雾测试；
- 是否可构建：是，Release为0警告0错误；
- 是否可测试：是，48项自动验证通过；
- 是否可体验：是，阶段1首个自包含体验包已生成；
- 建议下一步：请用户先体验待办重启保存；确认后再选择阶段1下一小增量。

## 阶段2追加记录：历史逐条操作

### 今日目标

- [x] 根据用户最新要求，结束“只能重新复制最新一条”的限制；
- [x] 在不引入剪贴板持久化的前提下，实现每条记录的独立复制和文字编辑。

### 已完成事项

- 用户确认进入下一阶段，路线图当前状态切换到阶段2；
- 历史列表由只保存摘要改为每张卡片持有独立的内存剪贴板载荷；
- 移除顶部“重新复制最新一条”，改为每张文字和图片卡片独立“复制”；
- 文字卡片增加行内编辑、保存、取消、Ctrl+Enter保存和Esc取消；
- 编辑后同步更新完整文字、摘要、搜索内容和复制指纹；
- 搜索由摘要匹配改为完整文字匹配；
- 图片卡片支持逐条重新复制，但不显示文字编辑入口；
- 自身重新复制回流抑制继续有效；
- 剪贴板载荷和编辑结果继续只保存在内存中，退出即清空。

### 验证与测试

- Release构建0警告、0错误；
- 55项自动验证通过；
- 新增验证独立载荷、文字编辑精确内容、摘要和指纹更新、完整文字搜索、图片编辑边界及空白编辑拒绝；
- 阶段2自包含体验包发布成功；
- UI自动烟雾测试通过：捕获两条不同文字、较早记录独立复制、较早记录编辑、编辑后再次复制及正常关闭均符合预期；
- 阶段2 EXE SHA-256：`CD735480F18F7C1F47DE5036CE9D6A87C44E38E90117B2E5CD568DAEE48F24E8`。

### 决策与变更

- 用户要求：剪贴板中每一条内容都可以重新复制或编辑，而不仅仅是最新一条；
- 本增量解释为：所有文字和图片记录均可逐条重新复制；文字可编辑；图片编辑器不属于第一版范围，本轮不实现；
- 本增量不提前实现剪贴板数据库、图片文件、置顶、删除和自动清理。

### 风险与问题

- 为支持旧图片重新复制，本版会在内存中保留列表内图片载荷；超大图片连续复制的内存压力仍需后续专项测试；
- 剪贴板历史退出即清空，尚未形成正式持久化闭环；
- 图片只能重新复制，不能裁剪、标注或修改。

### 待办事项

- [x] 发布阶段2体验包，并完成较早文字的复制、编辑后复制等核心UI烟雾验证；
- [ ] 用户体验卡片操作后，再推进下一小增量；
- [ ] 后续单独设计剪贴板持久化和图片文件生命周期。

### 结束状态

- 当前阶段：阶段2首个增量已完成；历史逐条操作、自动测试、发布和核心UI烟雾测试均已完成；
- 是否可构建：是；
- 是否可测试：是，55项自动验证通过；
- 是否可体验：是，阶段2自包含体验包已生成；
- 建议下一步：请用户体验每条卡片的复制与文字编辑手感，确认后再推进剪贴板持久化等下一小增量。


## 阶段4A追加记录：文字剪贴板持久化

### 今日目标

- [x] 先修复阶段3待办勾选完成顺序问题并完成回归；
- [x] 将SQLite升级到版本3并保存文字剪贴板历史；
- [x] 完成重复合并、逐条复制更新时间和文字编辑持久化；
- [x] 在文字落盘前加入首次隐私提示；
- [x] 完成自动验证、真实UI烟雾测试、文档和体验包。

### 已完成事项

- 待办完成流程改为先持久化状态与完成时间，再刷新今日待办和完成历史；保存失败恢复原状态；
- 新增 `clipboard_texts` 表，完整文字、SHA-256指纹、首次时间、最近复制时间和预留置顶状态写入本机SQLite；
- 启动加载全部文字历史，按最近复制时间降序显示；文字不再受30条内存上限限制；
- 相同文字连续复制、间隔复制或重启后再次复制均合并为一条，并更新时间；
- 点击任意文字卡片“复制”成功后更新时间并移到顶部，失败时不更新时间；
- 文字编辑同步更新完整内容、摘要、搜索内容、指纹和数据库；编辑成已有文字时自动合并；
- 数据库异常时不覆盖或删除原文件，文字历史回退为本次运行有效模式；
- 首次隐私提示说明本机保存、敏感文字风险、不上传云端、退出停止监听和历史不会随退出消失；未确认时不启动剪贴板监听；
- 图片继续只保存在本次运行内存中，最多30条，不写数据库和图片文件；
- 更新产品需求、路线图、界面规范、SQLite技术决策、体验说明和 `CLAUDE.md`。

### 验证与测试

- Release构建0警告、0错误；
- 79项自动验证全部通过；
- 覆盖SQLite v1→v3、v2→v3迁移，旧待办和完成历史不丢失；
- 覆盖文字新增、重启恢复、重复合并、排序、逐条Touch、编辑持久化和编辑重复自动合并；
- 覆盖应用设置隐私确认往返，确认设置文件不包含剪贴板正文；
- 真实UI烟雾测试通过：隐私确认、添加待办、真实鼠标勾选、完成历史、恢复未完成、删除测试待办、捕获唯一文字、逐条复制和重启恢复；
- UI烟雾测试使用临时本机数据目录，测试结束后已恢复用户原有数据和剪贴板；
- 280×320 DIP最小尺寸下隐私提示关键文字和两个操作按钮均在可见窗口范围内。
- 阶段4A EXE SHA-256：`218948D037DFB50607F961B64BB48E1FC1906769BCDF0E8AD6F9E286D98298AE`。

### 决策与变更

- 阶段4拆分为4A和4B，本轮只完成文字持久化与编辑闭环；
- `is_pinned`字段在版本3预留，但阶段4A不展示置顶功能；
- 编辑成重复文字采用默认“自动合并”；
- 隐私提示从阶段6提前到首次文字持久化之前；
- 图片持久化、文字置顶/删除和保存期限继续按后续独立增量推进。

### 风险与问题

- 阶段4A暂未提供文字删除、置顶和自动到期，不建议长期保存大量敏感内容；
- 文字数据当前未加密，能够访问当前Windows用户本机数据目录的人可能读取数据库；
- 图片退出后仍清空，尚未验证正式图片文件生命周期和磁盘清理；
- 数据库无正式恢复、备份、导入导出界面。

### 待办事项

- [x] 发布阶段4A自包含体验包并记录SHA-256；
- [ ] 用户体验阶段4A后，推进阶段4B：文字置顶、取消置顶和单条删除；
- [ ] 阶段5再实现图片持久化；
- [ ] 阶段6实现1天、2天、4天保存期限和自动清理。

### 结束状态

- 当前阶段：阶段4A已完成，下一步为阶段4B；
- 是否可构建：是，Release为0警告0错误；
- 是否可测试：是，79项自动验证和真实UI烟雾测试通过；
- 是否可体验：是，阶段4A自包含体验包已生成；
- 用户真实本机数据：烟雾测试后已恢复，未写入测试待办或测试剪贴板正文。
## 阶段4B追加记录：文字置顶与删除

### 今日目标

- [x] 只推进文字记录置顶、取消置顶和单条删除；
- [x] 明确置顶排序、重复复制和编辑合并规则；
- [x] 完成自动验证、最小窗口UI烟雾测试、文档和独立体验包。

### 已完成事项

- 文字卡片增加“置顶/取消置顶”和“删除”操作；
- 置顶记录显示“已置顶”标记并排在全部未置顶记录之前；同组内按最近复制时间降序；
- 置顶状态写入SQLite版本3现有 `is_pinned` 字段，重启后恢复；
- 相同文字重复复制时保留置顶状态；
- 编辑成重复文字自动合并时，任一原记录置顶，合并结果继续置顶；
- 单条删除先写数据库，成功后再移除卡片；失败时保留原卡片；
- 数据库不可用时置顶和删除仍可作为本次运行操作，并明确提示不会永久保存；
- 图片卡片不显示置顶或删除，继续保持内存边界；
- 卡片操作区改为可换行布局，避免280×320 DIP下按钮挤压正文；
- 应用页头、窗口标题和隐私说明更新为阶段4B。

### 验证与测试

- Release构建0警告、0错误；
- 85项自动验证全部通过；
- 新增覆盖置顶持久化、置顶优先排序、取消置顶、重复复制保留置顶、编辑合并保留置顶和单条永久删除；
- 最终自包含EXE真实UI烟雾测试通过：首次隐私确认、捕获文字、置顶、新文字不超过置顶记录、重启保留置顶、取消置顶恢复时间排序、单条删除和重启不恢复；
- 280×320 DIP最小尺寸下重复执行上述核心管理流程通过；
- UI测试使用临时本机数据目录，并在结束后恢复用户原有数据和系统剪贴板；
- 阶段4B EXE SHA-256：`A923712C6CF4AEE559A17A8E6AA4119E8A96D4B9BA89C90E8D05B39DEF7B85CF`。

### 决策与变更

- 置顶排序固定为“置顶组在前，各组内最近复制时间降序”；
- 重复复制不改变置顶状态；
- 编辑合并采用置顶并集规则，防止用户已置顶内容失去保护；
- 单条删除不增加二次确认；批量清空仍必须在阶段6提供确认；
- 阶段4B只管理文字，图片管理不提前进入本轮。

### 风险与问题

- 文字仍没有1天、2天、4天自动到期和批量清空；
- 图片仍在退出后清空，尚无正式图片文件生命周期；
- 本机SQLite数据未加密，敏感文字仍需谨慎保存；
- 删除没有撤销功能，后续如增加批量清空必须二次确认。

### 待办事项

- [x] 发布阶段4B独立自包含体验包并记录SHA-256；
- [ ] 请用户体验置顶、取消置顶和删除交互；
- [ ] 用户确认后进入阶段5，并先规划图片持久化的第一个小增量；
- [ ] 阶段6实现保存期限、自动清理和清空未置顶记录。

### 结束状态

- 当前阶段：阶段4文字剪贴板闭环已完成；
- 是否可构建：是，Release为0警告0错误；
- 是否可测试：是，85项自动验证和最小窗口真实UI烟雾测试通过；
- 是否可体验：是，阶段4B自包含体验包已生成；
- 用户真实本机数据：烟雾测试后已恢复，未留下测试文字或设置。
## 阶段5A追加记录：图片保存与圆角统一

### 今日目标

- [x] 统一功能卡片、输入框和图片缩略图容器的圆角；
- [x] 小步实现图片原图、缩略图和元数据的本机保存；
- [x] 完成图片去重、重启恢复和逐条重新复制；
- [x] 完成自动验证、真实UI烟雾测试、文档和独立体验包。

### 已完成事项

- 功能卡片统一为12 DIP圆角，输入框统一为10 DIP圆角，图片缩略图容器统一为12 DIP圆角；
- SQLite由版本3安全升级到版本4，新增图片元数据表，原有待办、完成历史、文字内容和文字置顶状态保持不变；
- 图片PNG原图和最长边不超过320像素的PNG缩略图保存到应用受管目录；
- 使用SHA-256图片指纹去重，相同图片再次复制不新增卡片，只更新最近复制时间；
- 软件重启后恢复图片记录和缩略图；点击任意图片卡片可按需读取原图并重新复制；
- 图片编码、缩略图生成和磁盘写入移到后台任务，并串行处理，减少界面卡顿和并发写入风险；
- 图片写入使用临时文件后安全移动；数据库插入失败时清理本次新建的原图和缩略图；
- 单张图片超过6000万像素或PNG编码后超过80MB时安全拒绝；单条失败不影响待办和文字历史。

### 验证与测试

- Release构建：0警告、0错误；
- 自动验证：94项全部通过；
- 覆盖SQLite v1/v2/v3到v4迁移、图片文件保存、元数据恢复、重复合并、原图读取和卡片原图引用；
- 真实UI烟雾测试通过：首次隐私确认、图片捕获、原图/缩略图生成、重启恢复和任意图片卡片重新复制原尺寸图片；
- UI测试结束后已恢复用户原有应用数据和系统剪贴板；
- 体验包：rtifacts\preview\phase5a-image-persistence-v1\HMinus.DesktopSpike.exe；
- EXE SHA-256：`758BF8CBAAA450EF1D9FFE0F6B7B609EDCCEB6F6787FC581E5F0C971E8FFCDE7`。

### 风险与问题

- 图片暂不支持置顶、取消置顶或单条删除；
- 尚无1天、2天、4天自动到期和批量清空；
- 本机SQLite和图片文件当前未加密；
- 不建议使用本体验版长期积累密码、验证码、密钥或其他敏感截图。

### 待办事项

- [x] 发布阶段5A独立自包含体验包并记录最终SHA-256；
- [ ] 请用户体验圆角、图片重启恢复和逐条重新复制；
- [ ] 用户确认后单独推进阶段5B：图片置顶、取消置顶、单条删除和文件删除一致性；
- [ ] 阶段6再实现保存期限、自动清理和清空未置顶记录。

### 结束状态

- 当前阶段：阶段5A已完成，下一步为阶段5B；
- 是否可构建：是，Release为0警告0错误；
- 是否可测试：是，94项自动验证和真实UI烟雾测试通过；
- 是否可体验：是，阶段5A自包含体验包已生成；
- 用户真实本机数据：烟雾测试后已恢复，未留下测试图片、文字或设置。


## 阶段5B追加记录：图片置顶与安全删除

### 今日目标

- [x] 只推进图片置顶、取消置顶和单条删除；
- [x] 确保数据库记录、原图和缩略图删除行为一致；
- [x] 增加删除中断恢复，不提前实现自动到期；
- [x] 完成自动验证、真实UI烟雾测试、文档和独立体验包。

### 已完成事项

- 图片卡片开放“置顶/取消置顶”和“删除”操作，文字卡片现有行为保持不变；
- 图片置顶状态写入SQLite v4现有 is_pinned 字段，重启后恢复；
- 置顶图片优先显示，同组内继续按最近复制时间降序；
- 相同图片再次复制时保留原置顶状态；
- 图片删除前将存在的PNG原图和缩略图移动为应用受管暂存文件；
- 数据库记录在事务中删除，数据库删除失败时恢复文件并保留界面卡片；
- 数据库删除成功后再清理暂存文件；最终清理未完成时明确提示，并在下次启动继续处理；
- 启动时依据数据库引用恢复删除中断但仍有效的文件，或清理已无数据库记录的暂存文件；
- 图片文件缺失时仍允许安全删除数据库记录；所有文件操作继续限制在应用受管目录内；
- 隐私提示和页头版本文字更新为阶段5B。

### 验证与测试

- Release构建：0警告、0错误；
- 自动验证：100项全部通过；
- 新增覆盖图片置顶持久化、重复复制保留置顶、取消置顶、删除中断恢复、图片记录永久删除及原图/缩略图同步删除；
- 真实UI烟雾测试通过：首次隐私确认、图片捕获、置顶、重启保留置顶、取消置顶、单条删除、文件消失和重启不恢复；
- UI测试结束后已恢复用户原有应用数据和系统剪贴板；
- 体验包：`artifacts\preview\phase5b-image-management-v1\HMinus.DesktopSpike.exe`；
- EXE SHA-256：`0F4641E28756DFC9BACA2AB14291778F04EAFD4019B9BFA52FBABFEBF1811F7C`。

### 决策与变更

- 阶段5B不升级数据库版本，直接启用v4图片表已预留的 is_pinned 字段；
- 单条删除不增加二次确认，与文字单条删除保持一致；批量清空仍必须在阶段6二次确认；
- 图片删除采用“文件暂存 → 数据库事务删除 → 最终文件清理”的失败安全顺序；
- 程序异常退出后的暂存文件按数据库引用恢复或清理，避免误删仍有效记录；
- 本轮不实现1天、2天、4天保存期限、自动清理或批量清空。

### 风险与问题

- 尚未完成较多图片记录下的内存、磁盘占用和列表滚动性能验证；
- 缺失或损坏图片目前显示失败状态，但没有正式修复或诊断界面；
- 本机SQLite和图片文件当前未加密；
- 自动到期尚未实现，不建议长期积累敏感截图。

### 待办事项

- [x] 发布阶段5B独立自包含体验包并记录最终SHA-256；
- [ ] 请用户体验图片置顶、取消置顶和单条删除；
- [ ] 用户确认后单独推进阶段5C：图片异常和性能收尾；
- [ ] 阶段6再实现保存期限、自动清理和清空未置顶记录。

### 结束状态

- 当前阶段：阶段5B已完成，下一步为阶段5C；
- 是否可构建：是，Release为0警告0错误；
- 是否可测试：是，100项自动验证和真实UI烟雾测试通过；
- 是否可体验：是，阶段5B自包含体验包已生成；
- 用户真实本机数据：烟雾测试后已恢复，未留下测试图片、文字或设置。



## v1.0.0正式封板与安装包追加记录

### 今日目标

- [x] 以阶段5B功能集封板，不增加阶段6业务功能；
- [x] 配置正式版本号、产品元数据和小狗应用图标；
- [x] 建立可重复的Inno Setup当前用户安装包；
- [x] 验证安装、覆盖安装、卸载保留数据和显式删除数据；
- [x] 完成最终UI烟雾、校验值和正式交付目录。

### 已完成事项

- 正式产品名称确定为“小狗效率屋”，版本为1.0.0；
- 程序内部EXE名称和 `%LocalAppData%\HMinus\DesktopSpike` 数据路径保持不变；
- 页头移除阶段体验版文字，更新为“v1.0.0 · 本机待办与剪贴板”；
- 从现有小狗参考图生成多尺寸应用图标，并写入程序和安装包；
- 使用Inno Setup 6.7.3生成中文、当前用户、无需管理员权限的安装包；
- 安装包创建开始菜单快捷方式，桌面快捷方式默认不勾选；
- 使用固定AppId支持后续覆盖升级；
- 卸载默认保留数据，显式选择时只删除固定应用数据目录；
- 新增正式发布、安装生命周期和正式UI测试脚本；
- 更新需求、路线图、技术、界面、质量、安全、SQLite决策和正式版使用说明。

### 验证与测试

- Release构建：0警告、0错误；
- 自动验证：100项全部通过；
- 安装生命周期：安装、启动、覆盖安装、开始菜单、默认无桌面快捷方式、单一卸载项、默认保留数据、显式删除数据和目录外文件保护全部通过；
- 测试删除使用隔离目录，没有删除用户真实应用数据。

### 决策与风险

- 保存期限、自动清理、批量清空和开机启动经用户确认延期，不属于v1.0.0；
- 本机数据未加密，且本版不会自动到期；
- 安装包未进行受信任代码签名，可能触发Windows SmartScreen；
- 当前机器只能验证Windows 11 x64，Windows 10 x64记为未实测。

### 正式版最终验证

- v1.0.0正式便携程序UI烟雾测试通过，测试后已恢复用户原有应用数据和系统剪贴板；
- 正式生产安装包独立安装、启动和默认保留数据卸载通过；
- Windows开始菜单和卸载项在测试结束后已清理；
- 最终SHA-256与发布目录见 `artifacts\release\小狗效率屋-v1.0.0-x64`。


### 正式交付校验值

- 安装包 SHA-256：`343B157DCC9286CC0C37241CC134AAF88CD608D6D32EB63115AC56CC8E69A971`；
- 便携程序 EXE SHA-256：`122A3EAA47924C4CA7A70E4F80E9F120A2F243728B5DBD512915AE191CB467B1`；
- 便携程序 ZIP SHA-256：`FDDB0D7B7F203429C2F07FBF0046C7DE11F4D95CD22D2D2C33B4E719C9E56AC2`；
- 安装包签名状态：未签名（NotSigned）；
- 正式发布目录：`artifacts\release\小狗效率屋-v1.0.0-x64`。

### 封板最终收尾

- 修正 `SHA256SUMS.txt` 为UTF-8编码，中文文件名可正常显示；
- 核对 `发布说明.md` 的Markdown格式与正式版限制说明；
- 最终哈希复算一致；
- 清理隔离安装测试空目录；
- 最终检查未发现测试安装包、测试目录、运行中程序、卸载项或开始菜单/桌面快捷方式残留；
- v1.0.0封板完成，可正式交付。
